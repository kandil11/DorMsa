# ==========================================================
# DorMsa Component Assembly & Wiring Configuration
# ==========================================================
# In Component-Based Programming (CBP), components are assembled
# and wired at deployment time using configuration.
#
# Define the active concrete class for each required interface:

# active EmailGateway component options:
# - gateway.MockEmailGateway (development)
# - gateway.SmtpEmailGateway (production / simulated SMTP)
EmailGateway=gateway.SmtpEmailGateway
